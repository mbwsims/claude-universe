{
  "name": "lenskit-mcp",
  "version": "0.2.0",
  "description": "MCP server for codebase complexity and risk analysis",
  "type": "module",
  "bin": {
    "lenskit-mcp": "./bin/mcp-server.mjs"
  },
  "files": [
    "dist",
    "bin"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc && esbuild dist/lenskit/src/mcp/server.js --bundle --platform=node --format=esm --banner:js=\"import{createRequire}from'module';const require=createRequire(import.meta.url);\" --outfile=dist/server.bundle.mjs",
    "dev": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "author": {
    "name": "Matt Sims",
    "url": "https://github.com/mbwsims"
  },
  "license": "MIT",
  "repository": "https://github.com/mbwsims/lenskit-mcp",
  "keywords": [
    "mcp",
    "codebase-analysis",
    "complexity",
    "claude-code"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0",
    "globby": "^14.1.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.7.0",
    "vitest": "~3.2.4"
  }
}
